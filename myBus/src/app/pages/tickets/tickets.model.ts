export interface tickets{
  expiretime: string; //se esiste data ricordati di metterci data al posto di String
  boughtime:string; //se esiste data ricordati di metterci data al posto di String
  price: number;
  code: string;
}
